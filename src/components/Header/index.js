import React from 'react';

const Header = () => (
  <header id="header" className="container">
    <div className="row">
      <div className="one-half columns">
        <h1>Scorekeeper</h1>
        <p><em>A small app to keep tab of scores in round based games.</em></p>
      </div>
    </div>
  </header>
);

export default Header;
